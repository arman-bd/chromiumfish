# ChromiumFish

A fingerprint-hardened fork of Chromium.

The fork is a small delta over upstream: source edits live in `patches/` and
binary overlays (icons, fonts) in `assets/`, applied onto a vanilla Chromium
checkout via `apply.sh`.

Early work in progress.